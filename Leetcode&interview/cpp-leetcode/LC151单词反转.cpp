#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
    string reverseWords(string s)
    {
        vector<char*> ans;
        const char* ss = s.c_str();
        int k = 0;
        do {
            char* aa;
            int rv = sscanf(&ss[k], "%s", &);

            k += rv;
        } while (rv > 0);
    }
};

int main()
{

    return 0;
}
